---
title: README — 项目入口
doc_type: template
layer: L1
description: L1 产品层 文档 README 的更新规范——修改 README.md 时触发，按模板 generation 元数据生成或更新该文档
globs:
  - "README.md"
# 生成提示词（元信息 · 仅模板持有，实例不含本块）
generation:
  # 自主判断（AI 扫描，不问用户）：当前项目路径 = 工作目录；是否已有 README = 扫描根目录
  scan:
    - 当前项目路径 = 工作目录（不问用户）
    - 扫描项目根，判断是否已有 README
    - 若已有：对照本模板结构判断是否符合（章节是否齐全）
  # 只有需要用户决策的才问（用询问工具）
  ask_user:
    - 生成时点：项目代码还没写（对话生成，用户描述需求）还是代码已写完（扫描项目自动生成）？让用户选
    - 已有 README → 参考/融入旧 README 的有效信息，但结构一定按本模板重建（不保留旧结构）
  flow: # 生成流程（分支）
    - 扫描项目（自主）：路径 + 已有/无 README
    - 问生成时点（对话生成 vs 项目自动生成）
    - 对话生成（代码未写）→ 与用户对话获取项目信息（一句话目标/技术栈/文档索引）→ 按模板新建
    - 项目自动生成（代码已写）→ 扫描项目（代码/文档/配置）→ 按模板生成
    - 已有 README → 融入旧 README 的有效信息（历史/背景/已知决策），但结构按本模板重建
  reentrant: # 可重入（不在乎 token，全量/增量都要支持）
    - 全量重生成：AI 收到"生成 README" → 无视已有结构，从模板完整重建（可融入旧 README 信息，但结构是新的）
    - 增量修改：已有 README 且符合模板 → 只更新变化部分（新增功能/技术栈变更/文档增删），保留未变内容
  notes: # 生成注意点
    - README 定位 = 项目是什么 + 技术栈 + 文档索引（只做入口，不承载内容）
    - 文档索引按 CONSTITUTION §3.1（L0-L4 + common 层），与实际文档一致
    - 技术栈只列事实，理由记 ADR
    - 与具体技术栈/框架无关（模板）
  checks: # 生成后反向 check
    - "README 定位正确（是什么+技术栈+索引，无冗余内容）"
    - "文档索引与实际文档一致（L0-L4 + common 层，路径正确）"
    - "技术栈与项目实际一致"
    - "README 与 CONSTITUTION §3.1 文档架构不冲突"
    - "S8：文档不含 emoji（grep 检查通过，详见 CONSTITUTION S8 依据）"
  related: # 关联模板与联动修改
    CONSTITUTION: 文档架构 SSOT 在它 §3.1，文档增删需同步 README 索引
    STRUCTURE: 目录结构 SSOT 在它 §1，目录变化需同步 README 路径
    USER-STORY: §5 UI 结构与交互点，新增 UI 时需同步
---

# <项目名> — <一句话定位>

> 本文档是「<项目名>」的**项目入口（README 模板）**。
> 【使用方式】复制为项目根 `README.md`，按指引填写。README 定位：**项目是什么 + 技术栈 + 文档索引**。
> 【原则】① 章节全保留，用不到留空；② `> 【指引】` 是填写说明，填写后删除；③ 与具体技术栈/框架无关。

---

## 1. 项目定位

> 【指引】本节说清"项目是什么、为什么做"。**一句话目标必须**；其余（背景/现状）按需。

### 1.1 一句话目标

> 句式：面向<人群>的<产品形态>，提供<核心能力>，解决<问题>。

<一句话目标>

### 1.2 背景与现状（可选）

> 【指引】可选：项目从哪来、当前做到哪。

<可选>

---

## 2. 技术栈

> 【指引】技术栈总结（语言/框架/平台/依赖）。只列事实，理由记 ADR。

| 类别      | 选型                 |
| --------- | -------------------- |
| 语言      | <如：TypeScript>     |
| 框架      | <如：微信原生小程序> |
| 平台/服务 | <如：微信云开发>     |
| 其他      | <补充>               |

---

## 3. 文档索引

> 【指引】按**文档架构（L0-L4 + common 层）**列出所有内容型文档。**本索引是"最全项目"的完整形态**——实际项目不一定有全部文档，没有的删掉对应行即可。README 自身不承载内容，只做索引。

### L0 决策

| 我想了解      | 看哪份文档                                                   |
| ------------- | ------------------------------------------------------------ |
| 项目宪法/规范 | `AGENTS.md`（宪法唯一真源在 `.omo/demo/L0/CONSTITUTION.md`） |

### L1 产品

| 我想了解      | 看哪份文档                   |
| ------------- | ---------------------------- |
| 项目入口/索引 | 本文件（README）             |
| 产品规格全景  | `docs/L1/PRODUCT.md`         |
| 用户故事      | `docs/L1/USER-STORY.md`      |
| UI/交互       | `docs/L1/USER-STORY.md` §4.2 |

### L2 架构

| 我想了解 | 看哪份文档                            |
| -------- | ------------------------------------- |
| 应用架构 | `docs/L2/APPLICATION-ARCHITECTURE.md` |
| 领域模型 | `docs/L2/DOMAIN-MODEL.md`             |
| 技术架构 | `docs/L2/TECHNOLOGY-ARCHITECTURE.md`  |

### L3 契约

| 我想了解                            | 看哪份文档                                                         |
| ----------------------------------- | ------------------------------------------------------------------ |
| API 接口（对外提供）                | `docs/L3/API.md`（总览）+ `docs/L3/` 下各 API 文档                 |
| 外部集成（被调用的第三方 API 文档） | `docs/L3/INTEGRATION.md`（总览）+ `docs/L3/` 下各外部 API 参考文档 |

### L4 交付

| 我想了解           | 看哪份文档                                |
| ------------------ | ----------------------------------------- |
| 测试计划/用例/报告 | `docs/L4/TEST-PLAN.md`                    |
| 部署与发布         | `docs/L4/DEPLOYMENT.md`（含版本发布记录） |

### common

| 我想了解                    | 看哪份文档                       |
| --------------------------- | -------------------------------- |
| 目录结构（文档 ↔ 代码映射） | `docs/common/STRUCTURE.md`       | 改文档前读取：定位文档对应代码，防漂移 |
| 术语表                      | `docs/common/GLOSSARY.md`        |
| 数据字典                    | `docs/common/DATA-DICTIONARY.md` |
| 安全设计                    | `docs/common/SECURITY.md`        |
| 技术债                      | `docs/common/TECHDEBT.md`        |
