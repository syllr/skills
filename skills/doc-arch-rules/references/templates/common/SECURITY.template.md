---
title: SECURITY — 安全设计
doc_type: template
layer: common
description: common 贯穿层 文档 SECURITY 的更新规范——修改 docs/common/SECURITY.md 时触发，按模板 generation 元数据生成或更新该文档
globs:
  - "docs/common/SECURITY.md"
# 生成提示词（元信息 · 仅模板持有，实例不含本块）
generation:
  tools:
    - Markdown 表格（威胁清单/密钥分层/脱敏规则）
  related: # 关联模板与联动修改
    CONSTITUTION: 安全红线/原则 SSOT 在它 §1（最高原则），本文件承接落地
    DEPLOYMENT: 密钥分层与注入规范 SSOT 在本文件 §6（见 notes），部署文档引用不复制；密钥/回调配置同步部署
    INTEGRATION: 外部服务鉴权与密钥管理需与集成契约一致（鉴权方式/凭据存储）
    API: 接口鉴权方案需与 openapi.yaml securitySchemes 一致（引用不复制）
    DATA-DICTIONARY: 敏感字段（密钥/凭据）登记元数据在它，脱敏规则在本文件
    GLOSSARY: 安全术语一致
  # 需要用户决策的才问（无歧义则不问）
  ask_user:
    - 安全等级/合规要求有明确外部约束时（等保/行业规范）→ 问用户采用哪级基线
  flow: # 生成流程
    - 扫描（自主）：读 CONSTITUTION §1（安全原则）+ TECHNOLOGY-ARCHITECTURE §4（外部依赖与鉴权）+ DEPLOYMENT（配置与密钥现状）+ 目标文档；扫描源缺失→以已有源+目标文档为准，不臆造
    - 已有 SECURITY → 参考旧文档有效信息，但结构按本模板重建
    - 按模板生成：§1 安全总则 → §2 认证与授权 → §3 数据安全 → §4 通信安全 → §5 敏感字段与脱敏 → §6 密钥分层与注入 → §7 审计与合规
  notes: # 生成注意点（怎么生成）
    - common 横切层：贯穿所有层的安全设计；**密钥分层与注入规范 SSOT 在本文件 §6**，DEPLOYMENT §6 引用不复制（严禁入库/落日志/落前端包，用密钥管理服务注入，支持轮换）
    - 敏感字段只写规则不写真实值：登记字段元数据（存在性/脱敏规则），真实值严禁写入文档（§6 当前态 + 安全红线）
    - 与 INTEGRATION 一致：外部服务鉴权方式（AppSecret/Token/签名）以集成契约为准，本文件不重复定义接口级鉴权（引用不复制）
    - 与 API 一致：接口鉴权方案以 openapi.yaml securitySchemes 为准（见 related），本文件只写安全设计原则与红线
    - 认证/授权/数据安全/通信安全按实际项目裁剪：无对应面（如无对外 Web 暴露）则标注「不适用」
    - 威胁清单含缓解措施：每项威胁 → 风险等级 → 缓解方案
  checks: # 生成后反向 check
    - "§6 密钥分层与注入规范存在且为 SSOT（DEPLOYMENT 引用不复制）"
    - "敏感字段仅元数据（无真实密钥/token/密码）"
    - "外部服务鉴权方式与 INTEGRATION 契约一致"
    - "接口鉴权方案与 openapi.yaml securitySchemes 一致"
    - "威胁清单含缓解措施（威胁→风险等级→缓解）"
    - "不适用项已标注（不硬撑）"
---

# SECURITY — 安全设计

> 本文档是「<项目名>」的 **SECURITY（安全设计）**——common 横切层的安全设计规范。
> 【模板使用指引】复制为 `docs/common/SECURITY.md`，按各章节指引填写。
> 【原则】① **贯穿所有层**的安全设计（认证/授权/数据/通信/密钥）；② **密钥分层与注入规范 SSOT 在本文件 §6**，其他文档引用不复制；③ 敏感字段只写规则不写真实值；④ 与 INTEGRATION/API 契约一致，不重复定义接口级鉴权。

---

## 1. 安全总则

> 【指引】安全原则与红线（对齐宪法 §1 最高原则）：如最小权限、纵深防御、密钥不入库/不落日志/不落前端包、传输加密。**威胁清单（威胁 → 风险等级 → 缓解方案）在本节**，与 notes/checks 对应。

- <安全原则与红线>

**威胁清单**：

| 威胁   | 风险等级   | 缓解方案   |
| ------ | ---------- | ---------- |
| <威胁> | <高/中/低> | <缓解方案> |

## 2. 认证与授权

> 【指引】认证方式（登录/SSO/token）、授权模型（RBAC/ACL）、会话管理；接口级鉴权方案以 openapi.yaml securitySchemes 为准（引用不复制）。

- <认证与授权设计>

## 3. 数据安全

> 【指引】敏感数据分类、加密存储（字段级/库级）、脱敏规则、备份与恢复；敏感字段元数据在 DATA-DICTIONARY。

| 数据类别 | 敏感等级   | 加密方式   | 脱敏规则   |
| -------- | ---------- | ---------- | ---------- |
| <类别>   | <高/中/低> | <加密方式> | <脱敏规则> |

## 4. 通信安全

> 【指引】传输加密（TLS）、证书管理、内网/公网暴露面；无对外暴露则标注「不适用」。

- <通信安全设计>

## 5. 敏感字段与脱敏

> 【指引】敏感字段登记（存在性 + 脱敏规则，**不写真实值**）；字段级定义在 DATA-DICTIONARY（引用不复制）。

| 敏感字段 | 所在文档/表 | 脱敏规则   | 备注   |
| -------- | ----------- | ---------- | ------ |
| <field>  | <位置>      | <脱敏规则> | <备注> |

## 6. 密钥分层与注入（SSOT）

> 【指引】**本文件是密钥分层与注入规范的唯一事实源**，DEPLOYMENT §6 等引用不复制。配置分层：代码内置默认值（兜底）→ 环境级配置（域名/开关）→ 运行期配置（热更新）→ 密钥（严禁入库/落日志/落前端包，用密钥管理服务注入，支持轮换）。

| 层             | 内容        | 管理方式       | 注入方式               |
| -------------- | ----------- | -------------- | ---------------------- |
| 代码内置默认值 | <默认值>    | <代码>         | <兜底>                 |
| 环境级配置     | <域名/开关> | <环境变量>     | <部署时注入>           |
| 运行期配置     | <热更新项>  | <配置中心>     | <运行时拉取>           |
| 密钥           | <严禁入库>  | <密钥管理服务> | <运行时注入，支持轮换> |

## 7. 审计与合规

> 【指引】操作审计、日志脱敏、合规要求（等保/行业规范）；有外部合规约束时按 ask_user 确认基线。

- <审计与合规要求>
