---
title: GLOSSARY — 术语表
doc_type: template
layer: common
description: common 贯穿层 文档 GLOSSARY 的更新规范——修改 docs/common/GLOSSARY.md 时触发，按模板 generation 元数据生成或更新该文档
globs:
  - "docs/common/GLOSSARY.md"
# 生成提示词（元信息 · 仅模板持有，实例不含本块）
generation:
  tools:
    - Markdown 表格（术语/含义/别称易混淆）
  scan: # 生成前自主扫描（不依赖用户）
    - 扫 docs/**/ 各文档术语（能力/功能/应用/模块/状态机/状态流转/接口/部署单元）
    - 扫 DOMAIN-MODEL §3 通用语言（领域术语，分工明确）
    - 扫旧 GLOSSARY（有效信息保留，结构按本模板重建）
  related: # 关联模板与联动修改
    DOMAIN-MODEL §1.2/§3: 领域特有术语在聚合设计内维护，新增需联动
    PRODUCT: 能力/功能术语，能力增删需同步术语
    CONSTITUTION: 文档分层规则 SSOT 在宪法 §3.1；图规范在 references/diagram-spec.md，术语统一需对齐
  # 需要用户决策的才问（无歧义则不问）
  ask_user:
    - 术语定义有争议时（如某词含义）→ 问用户
    - 术语冲突或新旧定义不一致 → 问用户裁决
  flow: # 生成流程
    - 按 scan 结果执行（扫描清单见上）
    - 已有 GLOSSARY → 复用 scan 结果重建结构；无旧文档则新建空表按本模板生成
    - 按模板生成：项目级术语表（含义/别称易混淆）
    - 清理：删除所有 `<!-- 按需 -->` 占位行与 `<...>` 示例
  notes: # 生成注意点（怎么生成）
    - 项目级通用术语只在本表维护（SSOT，见正文原则②），其他文档引用不复制；领域业务术语在 DOMAIN-MODEL §3 通用语言表维护（领域特有）——**分工唯一源：项目级通用 vs 领域特有**（见 related DOMAIN-MODEL）
    - 别称/易混淆词列出禁止混用
  checks: # 生成后反向 check
    - "术语覆盖项目级概念（能力/功能/应用/模块/状态机/状态流转）"
    - "与 DOMAIN-MODEL 聚合术语不冲突（项目级通用 vs 领域特有分工，见 related）"
    - "别称/易混淆词有禁止混用标注"
---

# GLOSSARY — 术语表

> 本文档是「<项目名>」的 **GLOSSARY（术语表模板）**——common 文档，贯穿所有层。
> 【模板使用指引】复制为 `docs/common/GLOSSARY.md`，按各章节指引填写。
> 【原则】① 概念级术语统一（跨文档/代码/对话使用同一词汇）；② **项目级通用术语只在本表维护**，其他文档引用不复制；**领域业务术语在 DOMAIN-MODEL §3 通用语言表维护**（领域特有，引用 GLOSSARY）；③ 别称/易混淆词列出禁止混用。④ 本表无元信息表、无变更记录。

---

## 1. 术语表

> 【指引】按层/域分组列出术语。**通用语言（Ubiquitous Language）**：业务专家与开发用同一套词汇。
> 【填写后删除所有 <!-- 按需 --> 占位行及 <...> 示例】

### 1.1 产品与功能层

| 术语                          | 含义                                    | 别称/易混淆                      |
| ----------------------------- | --------------------------------------- | -------------------------------- |
| 能力                          | 产品能力图节点                          | 功能（易混淆，见本表 §1.1 功能） |
| 功能                          | DOMAIN-MODEL 聚合操作(Action)描述的对象 | 能力（易混淆，见本表 §1.1 能力） |
| 能力域                        | 能力分组（例：<按产品能力图分组>）      | 模块（禁止混用）                 |
| <!-- 按需增行，无则删此行 --> |                                         |                                  |

### 1.2 架构层

| 术语                          | 含义                                | 别称/易混淆                              |
| ----------------------------- | ----------------------------------- | ---------------------------------------- |
| 应用                          | 可独立部署的单元（前端 / 后端 API） | 服务、模块（禁止混用）                   |
| 模块                          | 应用内部的功能实现单元              | 应用（禁止混用）                         |
| 领域对象                      | 业务建模对象（实体/值对象/聚合根）  | 数据对象（禁止混用）                     |
| 状态机                        | 领域对象的完整状态模型              | 状态流转（易混淆，见本表 §1.2 状态流转） |
| 状态流转                      | DOMAIN-MODEL §3 Action 流程图画片段 | 状态机（易混淆，见本表 §1.2 状态机）     |
| <!-- 按需增行，无则删此行 --> |                                     |                                          |

### 1.3 技术与数据层

| 术语                          | 含义                                              | 别称/易混淆      |
| ----------------------------- | ------------------------------------------------- | ---------------- |
| 接口                          | 对外提供的调用契约（L3 API 定义字段/类型/校验）   | 功能（禁止混用） |
| 部署单元                      | 最小可独立构建/发布/回滚的组件（DEPLOYMENT 登记） | 应用（禁止混用） |
| <!-- 按需增行，无则删此行 --> |                                                   |                  |
