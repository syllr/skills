---
title: DATA-DICTIONARY — 数据字典
doc_type: template
layer: common
description: common 贯穿层 文档 DATA-DICTIONARY 的更新规范——修改 docs/common/DATA-DICTIONARY.md 时触发，按模板 generation 元数据生成或更新该文档
globs:
  - "docs/common/DATA-DICTIONARY.md"
# 生成提示词（元信息 · 仅模板持有，实例不含本块）
generation:
  tools:
    - Markdown 表格（枚举/字段定义表）
  related: # 关联模板与联动修改
    DOMAIN-MODEL: 领域→存储映射索引在它 §5，字段级细节见本文件；全量字段在此展开；**事件定义在它 §4（各域小节内），本表不重复**
    DATA-ARCHITECTURE: 表/集合级结构在它 §5（物理存储格式），表清单与字典锚点双向引用
    API: 接口字段级定义见 openapi.yaml，本字典与之一致（引用不复制）
    INTEGRATION: 外部服务契约字段见 integration-contracts/，本字典与之一致（引用不复制）
    GLOSSARY: 概念级术语在它，本字典只管字段/枚举级
    SECURITY: 敏感字段（密钥/凭据）定义与脱敏规则在它 §5，本字典引用不复制
    DATA-ARCHITECTURE: 独立成篇（L2），表/集合级结构见它 §5，本字典字段行链回其表清单锚点
  # 需要用户决策的才问（无歧义则不问）
  ask_user:
    - 字段/枚举口径有跨系统冲突且无法从代码定论时（如两处定义不一致）→ 问用户以哪个为准
  flow: # 生成流程
    - 扫描（自主）：读 DATA-ARCHITECTURE §5（表/集合级）+ DOMAIN-MODEL §5（映射索引）+ 代码实体/DTO/枚举定义 + 数据库 schema + 目标文档；扫描源缺失→以已有源+目标文档为准，不臆造
    - 已有 DATA-DICTIONARY → 参考旧文档有效信息，但结构按本模板重建
    - 按模板生成：§1 枚举定义（SSOT 汇总，按枚举域分组）→ §2 核心表字段定义（4 列表）→ §3 编号格式约定（待定稿）→ §4 相关文档
  notes: # 生成注意点（怎么生成）
    - 字段/枚举级定义见本文件：全量字段、枚举值在此维护；DATA-ARCHITECTURE 只写表/集合级结构，不重复全量字典
    - **§1 枚举为 SSOT 汇总**：按枚举域分组小节，每小节含来源标注行（枚举定义源头：代码枚举类 / DOMAIN-MODEL §3.x 状态机 / 契约文件）；补「状态机取值汇总」小节，与 DOMAIN-MODEL §3.x 各域状态机取值一致（宪法 通用语言贯穿）
    - **§2 核心表字段定义**：每表含来源 note 行（表来源：DATA-ARCHITECTURE §5 表清单锚点 / 代码实体 / 契约）；字段表 4 列（字段/类型/约束/说明），**必填与默认并入约束列**；表末加索引行占位（索引名/字段/唯一性）
    - **§3 编号格式约定**：业务编号（单号/编码）的生成规则与格式，**待定稿**（未定稿时保留占位，不臆造）
    - 与代码实体/DTO 一致：字段增删改同步本字典（通用语言贯穿）
    - 敏感字段（密钥/token/脱敏）只登记元数据（存在性/脱敏规则/引用 SECURITY），不写真实值
    - 与 GLOSSARY 分工：概念级术语在 GLOSSARY，本文件只管字段/枚举级
    - 与 API/integration-contracts 契约一致：接口字段以 openapi.yaml / 契约文件为准，本字典不重复定义契约字段（引用不复制）
    - **事件定义不在此**：领域事件定义在 DOMAIN-MODEL §4（各域小节内），本表不重复，仅 §1 声明行指向
  checks: # 生成后反向 check
    - "§1 枚举按枚举域分组，每小节含来源标注行；含「状态机取值汇总」小节且与 DOMAIN-MODEL §3.x 状态机取值一致"
    - "枚举定义含全部取值与含义（无遗漏）"
    - "§2 字段表为 4 列（字段/类型/约束/说明），必填与默认并入约束列；每表含来源 note 行；表末含索引行占位"
    - "§3 编号格式约定已填或标注待定稿（不臆造）"
    - "§4 相关文档含 GLOSSARY/DOMAIN-MODEL/TECHNOLOGY/STRUCTURE 链接"
    - "无事件定义节（事件在 DOMAIN-MODEL §4，本表仅声明行指向）"
    - "与 DATA-ARCHITECTURE §5 表/集合级一致（无字段级重复、无冲突）"
    - "与代码实体/DTO 定义一致（grep 对照）"
    - "敏感字段未写真实值（仅元数据 + 引用 SECURITY）"
    - "术语与 GLOSSARY 一致（通用语言贯穿）"
    - "章节编号连续且顺序递增（无跳号、无乱序）"
    - "与 API/integration-contracts 契约一致（契约已定义字段不重复，引用不复制）"
---

# DATA-DICTIONARY — 数据字典

> 本文档是「<项目名>」的 **DATA-DICTIONARY（数据字典）**——common 贯穿层的枚举/字段级数据定义。
> 【模板使用指引】复制为 `docs/common/DATA-DICTIONARY.md`，按各章节指引填写。
> 【原则】① **枚举/字段级定义见本文件**：全量枚举/字段在此维护；② 表/集合级结构在 DATA-ARCHITECTURE §5，此处只管字段级细节；③ 概念级术语在 GLOSSARY；④ 敏感字段只登记元数据不写真实值；⑤ **事件定义不在此**——领域事件在 DOMAIN-MODEL §4（各域小节内），本表不重复，仅 §1 声明行指向。

---

## 1. 枚举定义（SSOT 汇总）

> 【指引】本表为**枚举的 SSOT 汇总**——按枚举域分组小节，每小节含**来源标注行**（枚举定义源头：代码枚举类 / DOMAIN-MODEL §3.x 状态机 / 契约文件）。取值变化时同步代码与 DOMAIN-MODEL §3.x 各域小节内状态机（宪法 通用语言贯穿）。接口契约枚举（openapi.yaml / integration-contracts/ 已定义）不重复，引用不复制。

### 1.1 <枚举域 1>

> 来源：<如 代码 `enum <X>` / DOMAIN-MODEL §3.x 状态机 / 契约文件>

| 枚举   | 取值              | 含义   |
| ------ | ----------------- | ------ |
| <enum> | <value1>/<value2> | <含义> |

### 1.2 <枚举域 2>

> 来源：<如 代码 `enum <Y>` / DOMAIN-MODEL §3.x 状态机 / 契约文件>

| 枚举   | 取值              | 含义   |
| ------ | ----------------- | ------ |
| <enum> | <value1>/<value2> | <含义> |

### 1.3 状态机取值汇总

> 【指引】汇总各领域对象状态机的全部取值（与 DOMAIN-MODEL §3.x 各域小节内状态机一致，通用语言贯穿）；状态转移语义见 DOMAIN-MODEL，本表只汇总取值。

| 领域对象 | 状态取值（全部）        | 状态机位置 |
| -------- | ----------------------- | ---------- |
| <对象>   | <状态1>/<状态2>/<状态3> | <§3.x>     |

> **事件定义声明**：领域事件定义在 DOMAIN-MODEL §4（各域小节内），本表不重复，事件载荷见 DOMAIN-MODEL §3.x 各域小节「本域事件」。

---

## 2. 核心表字段定义

> 【指引】按数据域/表/集合分组（与 DATA-ARCHITECTURE §5 结构对应）。每表含**来源 note 行**（表来源：DATA-ARCHITECTURE §5 表清单锚点 / 代码实体 / 契约）；字段表 **4 列**（字段/类型/约束/说明），**必填与默认并入约束列**；表末加**索引行占位**。接口契约字段（openapi.yaml / integration-contracts/ 已定义）不重复，引用不复制。

### 2.1 <表/集合名>

> 来源：<如 DATA-ARCHITECTURE §5 表清单锚点 / 代码实体 `<Entity>` / 契约文件>

| 字段    | 类型   | 约束（必填/默认/唯一/外键…） | 说明   |
| ------- | ------ | ---------------------------- | ------ |
| <field> | <type> | <如 必填，默认 <default>>    | <说明> |

**索引**：

| 索引名 | 字段       | 唯一性 |
| ------ | ---------- | ------ |
| <idx>  | <字段列表> | <Y/N>  |

---

## 3. 编号格式约定（待定稿）

> 【指引】业务编号（单号/编码）的生成规则与格式（前缀/日期/序号/校验位等）。**待定稿**——未定稿时保留占位，不臆造；定稿后按项目实际填充。

- 编号规则：<如 前缀 + 日期 + 序号，示例 `INV-20260901-0001`>
- 生成方式：<如 数据库自增 / 分布式 ID / 业务规则生成>
- 唯一性约束：<如 全局唯一 / 按域唯一>

---

## 4. 相关文档

- [GLOSSARY](../GLOSSARY.template.md)：概念级术语（本表只管字段/枚举级）
- [DOMAIN-MODEL](../DOMAIN-MODEL.template.md)：领域→存储映射索引（§5）、状态机（§3.x）、事件定义（§4）
- [TECHNOLOGY-ARCHITECTURE](../TECHNOLOGY-ARCHITECTURE.template.md)：存储选型（§3.1/§4）
- [STRUCTURE](../STRUCTURE.template.md)：目录结构（文档 ↔ 代码映射）
