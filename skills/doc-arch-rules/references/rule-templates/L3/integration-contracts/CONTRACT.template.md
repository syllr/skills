---
title: CONTRACT — 外部服务契约（Outbound）
doc_type: template
layer: L3
description: L3 契约层 外部服务契约文件的更新规范——修改 docs/L3/integration-contracts/ 下任一契约文件时触发，按模板 generation 元数据生成或更新该外部服务契约
globs:
  - "docs/L3/integration-contracts/**"
# 生成提示词（元信息 · 仅模板持有，实例不含本块）
generation:
  tools:
    - Markdown 表格（接口列表/出入参/错误码）
    - Mermaid sequenceDiagram（集成形态时序图，见 references/diagram-spec.md 时序图规范）
  related: # 关联模板与联动修改
    INTEGRATION: 说明书见它 §1/§2（总览与概览），本契约文件的接入/状态变化需同步说明书（不复制接口/字段/错误码）
    TECHNOLOGY-ARCHITECTURE: 外部依赖见它 §4，选型变化需同步本契约
    DOMAIN-MODEL: ACL Adapter 在总文档 §5 Mapper，字段术语需与领域模型对齐；时序图的 Port/Adapter 名须能在总文档 §5 与 APPLICATION-ARCHITECTURE §3.1 定位
    DATA-ARCHITECTURE: 外部数据资产见它 §2/§5.6，字段术语来源
    DEPLOYMENT: 外部服务密钥/回调需同步部署配置
  # 需要用户决策的才问（无歧义则不问）
  ask_user:
    - 外部服务选择有争议时（如选哪个 AI 供应商）→ 问用户
  flow: # 生成流程
    - 扫描（自主）：读 TECHNOLOGY-ARCHITECTURE §4 外部依赖 + INTEGRATION §1 总览（服务定位）+ DATA-ARCHITECTURE §2/§5.6 外部数据资产（字段术语来源）+ DOMAIN-MODEL 总文档 §5 Mapper（翻译标准）+ 目标契约文件；扫描源缺失→以已有源+目标文档为准，不臆造
    - 已有契约文件 → 参考旧文档有效信息，但结构按本模板重建
    - "INDEX.md 判别：globs 命中 `integration-contracts/INDEX.md` 时，按 [INDEX](INDEX.template.md) 骨架维护（目录唯一入口：新增/删除契约文件同步清单表，宪法 §3.2 目录索引约定），不生成本模板正文"
    - "INDEX.md 同步义务（独立于上方判别分支）：新增/删除契约文件后同步 INDEX.md 清单表（宪法 §3.2 第2条，与文档同交付、漏更即未完成）——globs 未命中 INDEX.md 时本义务仍触发"
    - 按模板生成：§1 集成形态（同构时序图）→ §2 接入方式（契约状态 + 接入细节）→ §3 接口列表（调用面）→ §4 接口定义（每接口三件套）→ §5 错误码（全量集中）→ §6 调用方 → §7 复杂服务可选节（按需）
  notes: # 生成注意点（怎么生成）
    - "一服务一契约文件：文件名为 `<service>.md` kebab-case 服务名（如 llm-api.md / vector-service.md），与 INTEGRATION §2 各服务小节「契约文件」行一一对应"
    - "字段术语见本契约文件：外部服务字段级定义/语义/错误码只在本文件维护，说明书引用不复制；跨文件术语冲突以本契约文件为准（防 project_id/file_id ↔ kb_id/doc_id 类冲突）"
    - "接口列表 = 本系统调用面：§3 只列当前系统实际调用的接口（外部服务完整 API 以其官方文档为准，留引用位即可，不替对方维护全集）；调用面变化 = 契约变更，两处同步——§3 加行 + §4 补三件套小节（§1 时序图消息为领域实体概括，仅在调用方实体/场景变化时同步）"
    - "时序图同构骨架：§1 必含 Mermaid 时序图，四 participant 固定（<调用方业务层> / <XxxPort>(domain 接口) / integration Adapter(防腐层) / <外部服务>），只换名字与消息；Biz→Port 消息写调用方领域实体/业务场景概括（不逐接口枚举——接口全量在 §3；实线=调用、虚线=返回/翻译），概括与 §6 调用方一致；Port/Adapter 名须能在 DOMAIN-MODEL 总文档 §5 与 APPLICATION-ARCHITECTURE §3.1 定位"
    - "场景映射保持表格形态：§7.2 只留「驱动场景 → 触发接口 → 说明」紧凑表；业务场景长篇叙述/流程图不进契约——高复杂度场景语义下沉 L2/deep-dives 单篇（命中 INDEX 判定即单列登记），契约侧一行提及；接口级硬性约定统一归 §7.5，不在场景说明中重复"
    - "接口三件套：§4 每接口一小节，结构固定 = 接口语义（用途 + 幂等性/重复调用语义）→ 方法签名（HTTP 写 `METHOD /path`，SDK/Port 写函数签名）+ 输入表 + 返回表 → 失败处理（本接口特殊的失败行为：重试/状态回写/降级/部分成功等，强调与 §2 失败策略的差异；错误码统一登记 §5，接口小节不重复列码表）"
    - "错误码集中 §5：本契约全量错误码（公共 + 接口特有）集中在 §5 一张表，「适用」列标注范围（全接口 / 具体接口名）；接口小节与 §2 接入方式均不重复列错误码表，只做失败处理语义强调"
    - "契约状态必填：`mock 中 / 已交付 / 已上线` 三态；mock 实现（如 InMemoryXxxAdapter）切换真实服务时替换 adapter，业务代码零改动；契约状态为「已上线」时省略 §2 的 mock 实现/切换方式字段"
    - "三节分工（生成时防互抄）：§1 讲「对接什么 + 经哪个 adapter 统一翻译」；§2 讲「怎么接」——契约状态（谁在履行/怎么切换）+ 真实接入形态（分型/地址/鉴权/失败策略）；§3/§4 讲「调用面与逐接口契约」。mock 返回的示例数据集在 §2「mock 实现」行说明，连接配置/鉴权在 §2 接入细节行说明（mock 阶段未启用时显式注明「切换真实实现后生效」）"
    - §6 调用方按业务层/模块粒度填写；应用级归属由 INTEGRATION §1 总览维护（须在 APPLICATION-ARCHITECTURE §2.2 可定位），本文件引用不重复
    - 字段类型与代码 DTO/契约定义一致（引用不复制）
    - "复杂服务可选节：我方定义契约且接口/字段复杂时启用 §7（背景与设计决策/接口场景映射/设计理由/方案对比/关键语义/当前实现），简单服务省略"
  checks: # 生成后反向 check
    - "§1 时序图存在且为四 participant 同构骨架（业务层/XxxPort(domain 接口)/integration Adapter(防腐层)/外部服务）"
    - "时序图 Biz→Port 消息为调用方领域实体/场景概括（不逐接口枚举，接口全量在 §3），概括与 §6 调用方一致（无悬空）"
    - "§7.2 为紧凑映射表，无业务场景长篇叙述/流程图（高复杂度场景语义下沉 L2/deep-dives，契约侧一行提及；硬性约定在 §7.5）"
    - "§3 接口列表只含本系统调用面（不替对方维护全集），方法签名双形态（HTTP: METHOD /path；SDK: 函数签名）"
    - "§4 每接口三件套齐全（接口语义含幂等性 / 方法签名+输入表+返回表 / 失败处理含特殊行为强调）"
    - "§5 错误码章集中全量错误码（公共 + 接口特有一张表，「适用」列标注范围）；§2 接入方式与 §4 接口小节不重复列错误码表"
    - "§5 错误码覆盖 §4 各接口失败处理中引用的错误码（无死引用）"
    - "§2 契约状态字段存在且值为 mock 中/已交付/已上线 三态之一"
    - "文件名 kebab-case，与 INTEGRATION §2 各服务小节「契约文件」行一致"
    - "§6 调用方为业务层/模块粒度，应用级归属经 INTEGRATION §1 总览可在 APPLICATION-ARCHITECTURE §2.2 定位（无悬空）"
    - "字段术语与 DATA-ARCHITECTURE §2/§5.6 及 DOMAIN-MODEL 总文档 §5 一致（无旧术语残留）"
    - "INTEGRATION 说明书不列接口/字段/错误码（§2 无内联表，一律查契约文件）"
    - "契约状态为「已上线」时 §2 省略 mock 实现/切换方式字段"
    - "迁移/删除后全仓 grep 旧名残留 = 0（STRUCTURE/README/上游文档/INTEGRATION 总览/INDEX.md 已同步）"
---

# <外部服务名> 契约

> 本文档是「<项目名>」外部服务 <服务名> 的契约文件——`docs/L3/integration-contracts/<service>.md`（kebab-case 服务名，如 llm-api.md）。
> 【模板使用指引】复制为契约文件，按各章节指引填写；本文件是字段级契约的维护处，INTEGRATION 说明书只引用不复制。
> 【原则】① 一服务一契约：字段术语/接口/错误码只在本文件维护；② 接口列表只覆盖本系统调用面，不替对方维护全集；③ 契约状态标注 mock/真实切换；④ 无元信息表、无变更记录。

---

## 1. 集成形态

> 【指引】先用一段话说清：对接什么、经哪个 ACL 防腐层 adapter（给代码路径）统一连接与翻译；再画同构时序图（骨架/消息约定单源见生成 notes「时序图同构骨架」条，本节不重复措辞）。接入细节不进本节：协议分型/地址/鉴权/失败策略归 §2 接入方式，本段不展开、不逐字复述。

<对接内容一句话：如「系统内置一个默认业务数据源，需接入真实业务数据供抽取同步」；经 `<集成层代码路径>`（ACL 防腐层）统一连接与抽取，防止外部数据模型污染内部模型。>

```mermaid
sequenceDiagram
    participant Biz as <调用方业务层>
    participant Port as <XxxPort>(domain 接口)
    participant ACL as integration Adapter(防腐层)
    participant Ext as <外部服务名>
    Biz->>Port: <领域实体调用（实体A / 实体B——场景）>
    Port->>ACL: 接口方法调用
    ACL->>Ext: <驱动调用（协议/SDK）>
    Ext-->>ACL: <外部原始返回>
    ACL-->>Port: 翻译为 domain 模型
    Port-->>Biz: 返回结果
```

---

## 2. 接入方式

> 【指引】本节集中「怎么接」：先契约状态（谁在履行 + 怎么切换），再真实接入形态（分型/地址/鉴权/失败策略）。契约状态为「已上线」时省略 mock 实现/切换方式两项（仅保留契约状态）。mock 返回的示例数据集在「mock 实现」行说明；接入细节描述的是切换后的生效形态——mock 阶段未启用时在地址/鉴权行显式注明。错误码统一见 §5 错误码章（本节不列错误码表）。

- 契约状态：<mock 中 / 已交付 / 已上线>
- mock 实现：<如 InMemory<Xxx>Adapter，内置示例数据集/模拟行为>（契约状态为「已上线」时省略）
- 切换方式：<替换 Adapter 实现类（推荐，业务代码零改动）/ 修改连接配置指向真实服务；业务代码零改动>（契约状态为「已上线」时省略）
- 接入类型：<协议/驱动分型，如 API（HTTP/REST）/ SDK / Webhook>
- 接入地址：<base URL / 端点 / 连接配置来源；mock 阶段未启用时注明「切换真实实现后生效」>
- 鉴权：<AppSecret / Token / 签名>（必填；密钥经部署配置注入见 DEPLOYMENT；mock 阶段未启用时注明）
- 失败策略：<超时<毫秒数>ms / 重试<次数>次 / 降级<策略>>（必填，示例：超时5000ms/重试3次/降级返回缓存）

---

## 3. 接口列表（本系统调用面）

> 【指引】本表只列当前系统实际调用的接口（调用子集）——外部服务完整 API 以其官方文档为准（<外部 API 文档链接，无则此句省略>），不替对方维护全集。调用面变化 = 契约变更：同步本表 + §4 对应小节（两处一致；§1 时序图消息为领域实体概括，仅在调用方实体/场景变化时同步）。

| 接口名   | 方法签名                                            | 说明   |
| -------- | --------------------------------------------------- | ------ |
| <接口名> | <HTTP：`METHOD /path`；SDK/Port：`fn(args) -> Ret`> | <说明> |

---

## 4. 接口定义

> 【指引】每接口一小节，三件套结构固定：① 接口语义（用途一句话 + 幂等性/重复调用语义）；② 方法签名与出入参（签名 + 输入表 + 返回表）；③ 失败处理（本接口特殊的失败行为与处理——重试/状态回写/降级/部分成功等，强调与 §2 失败策略的差异；错误码统一登记 §5，本小节不重复列码表）。字段术语与 DATA-ARCHITECTURE §2/§5.6 及 DOMAIN-MODEL 总文档 §5 对齐。

### 4.1 <接口名>

接口语义：<用途一句话；幂等性与重复调用语义>

方法签名：<HTTP：`METHOD /path`；SDK/Port：`fn(args) -> Ret`>

输入：

| 参数    | 类型   | 必填  | 说明   |
| ------- | ------ | ----- | ------ |
| <param> | <type> | <Y/N> | <语义> |

返回：

| 字段    | 类型   | 说明   |
| ------- | ------ | ------ |
| <field> | <type> | <语义> |

失败处理：<本接口特殊的失败行为与处理（重试 / 状态回写 / 是否阻塞其他链路 / 部分成功处理）；涉及错误码时直接引用 §5 错误码章>

（每接口一小节，按 §3 接口列表逐个展开）

---

## 5. 错误码

> 【指引】本契约全量错误码集中此处（公共 + 接口特有，一张表），「适用」列标注范围（全接口 / 具体接口名）——§2 接入方式与 §4 接口小节均不重复列码，只做失败处理语义强调。「处理」列给动作（重试/中止/降级/排查配置）。

| 错误码   | 说明   | 适用     | 处理   |
| -------- | ------ | -------- | ------ |
| 鉴权失败 | <含义> | 全接口   | <处理> |
| <code>   | <含义> | <接口名> | <处理> |

---

## 6. 调用方

- 调用方：<调用方业务层/模块（应用级归属见 INTEGRATION §1）>

---

## 7. 我方定义契约的复杂服务（可选节）

> 【指引】可选节，仅复杂服务使用：当本契约由我方定义（非第三方给定）、且接口/字段复杂、需向外部团队交付实现时，用本节承载设计决策与语义约定。简单服务可省略本节。

### 7.1 背景与设计决策

> 【指引】说明本契约的产生背景与关键设计决策，供外部团队理解「为什么这么设计」。

| 决策项   | 决策内容 | 理由   |
| -------- | -------- | ------ |
| <决策项> | <决策>   | <理由> |

### 7.2 接口场景映射

> 【指引】将接口映射到业务驱动场景，说明每个接口在什么场景下被调用。保持紧凑表格形态——业务场景的长篇叙述（逐场景展开为什么）与流程图不进契约：高复杂度场景语义/链路图下沉 L2/deep-dives 单篇（命中 INDEX 判定即单列登记），契约侧一行文字提及；接口级硬性约定归 §7.5，不在场景说明里重复。

| 驱动场景 | 触发接口 | 说明   |
| -------- | -------- | ------ |
| <场景>   | <接口>   | <说明> |

### 7.3 设计理由

> 【指引】阐述接口/字段设计的核心理由（为何这样拆分、为何这样命名、边界如何划分）。

- <设计理由>

### 7.4 方案对比

> 【指引】列出备选方案与选定方案的对比（三方案表），说明为何选当前方案。

| 方案   | 优点   | 缺点   | 结论    |
| ------ | ------ | ------ | ------- |
| 方案 A | <优点> | <缺点> | <选/弃> |
| 方案 B | <优点> | <缺点> | <选/弃> |
| 方案 C | <优点> | <缺点> | <选/弃> |

### 7.5 关键语义（外部团队硬性约定）

> 【指引】本契约对外部团队实现的硬性约定（不可违背的语义/约束），外部团队必须遵守。

- <关键语义约定>

### 7.6 当前实现

> 【指引】标注当前实现状态与代码位置（File:Line）。

- 当前实现：<实现位置 / 状态>
