---
description: common 贯穿层 文档 GLOSSARY 的全局规范——术语表全局约束，始终加载
alwaysApply: true
---

# GLOSSARY 文档更新规范（common 贯穿层）

**本文档在修改 `docs/common/GLOSSARY.md` 时生效。** 目标：按下方模板生成/更新 `docs/common/GLOSSARY.md`，使其结构符合模板契约，保持 SSOT、不漂移、不遗漏联动。

## 触发条件

当以下任一情况发生时，本规则必须生效：

- 编辑、新增或重建 `docs/common/GLOSSARY.md`
- 该文档关联的其他文档（见模板 `related`）发生变化，需要联动更新本文档
- 用户要求"生成/更新 GLOSSARY"

## 执行流程

1. **读模板 generation 元数据**：下方「模板全文」的 frontmatter `generation` 块是本文档的"生成/更新提示词"，逐字段执行：
   - `scan`：自主扫描列出的源（不问用户），作为更新依据
   - `ask_user`：仅当列出的决策点存在歧义时，才用询问工具问用户
   - `flow`：按列出的流程分支执行（全量重建 or 增量修改）
   - `reentrant`：支持可重入——全量重生成或增量修改都要能处理
   - `notes`：注意点（怎么生成，避免常见错误）
   - `checks`：生成后逐条反向核对（含 S8：文档不含 emoji）
   - `related`：关联模板与联动修改——更新本文档时，检查并同步 `related` 列出的关联文档
2. **按模板正文生成**：以下方「模板全文」的 Markdown 正文为结构基准，把模板复制为 `docs/common/GLOSSARY.md`，按 `> 【指引】` 填写，**删除 generation 元数据块与全部 `> 【指引】` 说明**（实例不含这两者）。
3. **反向 check**：逐条执行模板 `generation.checks`，全部通过才算完成。

## 硬性要求

- **SSOT**：模板是本文档的唯一结构源；已合并/已删除的模板（如 DATA-ARCHITECTURE 已并入 DOMAIN-MODEL）不生成独立文档。
- **不用 emoji**（S8，grep 校验：`grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" <文档>`）。
- **联动**：`related` 列的关联文档必须同步检查；跨层引用单向向下，下层不链回上层。
- **图规范**：文档中的图按模板要求用 D2 / Mermaid / ASCII，绘制规范见 CONSTITUTION §3.2。

## 完成判定

模板 `generation.checks` 全部通过 + 文档与关联文档无漂移。

---

## 模板全文（本 rule 的生成依据）

以下是 `GLOSSARY` 的完整模板（frontmatter generation 元数据 + Markdown 正文，SSOT，来自 references/templates/common/GLOSSARY.template.md）：

```markdown
---
title: GLOSSARY — 术语表
doc_type: template
layer: common
description: common 贯穿层 文档 GLOSSARY 的全局规范——术语表全局约束，始终加载
alwaysApply: true
# 生成提示词（元信息 · 仅模板持有，实例不含本块）
generation:
  # 自主扫描（AI 读源，不问用户）
  scan:
    - 扫描各文档：提取项目级通用术语（能力/功能/应用/模块/状态机/状态流转）
    - 读 DOMAIN-MODEL §3：领域术语（分工参考）
    - 扫描目标文档：GLOSSARY 是否已存在
  # 需要用户决策的才问（无歧义则不问）
  ask_user:
    - 术语定义有争议时（如某词含义）→ 问用户
  flow: # 生成流程
    - 扫描（自主）：扫各文档术语 + 领域术语 + 目标文档
    - 已有 GLOSSARY → 参考旧文档有效信息，但结构按本模板重建
    - 按模板生成：项目级术语表（含义/别称易混淆）
  reentrant: # 可重入（全量/增量）
    - 全量重生成：收到"生成 GLOSSARY" → 从模板 + 扫描术语完整重建
    - 增量修改：已有且符合模板 → 只更新变化术语，保留未变
  tools:
    - Markdown 表格（术语/含义/别称易混淆）
  notes: # 生成注意点（怎么生成）
    - 项目级通用术语只在本表维护（SSOT），其他文档引用不复制
    - 领域业务术语在 DOMAIN-MODEL §3 通用语言表维护（领域特有）
    - 别称/易混淆词列出禁止混用
    - 不用 emoji
  checks: # 生成后反向 check
    - "术语覆盖项目级概念（能力/功能/应用/模块/状态机/状态流转）"
    - "与 DOMAIN-MODEL §3 领域术语不冲突（分工明确）"
    - "别称/易混淆词有禁止混用标注"
    - "S8：文档不含 emoji（grep 检查通过，详见 CONSTITUTION S8 依据）"
  related: # 关联模板与联动修改
    DOMAIN-MODEL: 领域术语在它 §3，新增领域术语需联动
    PRODUCT: 能力/功能术语，能力增删需同步术语
    CONSTITUTION: 图规范在它 §3.2，术语统一需对齐
---

# GLOSSARY — 术语表

> 本文档是「<项目名>」的 **GLOSSARY（术语表模板）**——common 文档，贯穿所有层。
> 【模板使用指引】复制为 `docs/common/GLOSSARY.md`，按各章节指引填写。
> 【原则】① 概念级术语统一（跨文档/代码/对话使用同一词汇）；② **项目级通用术语只在本表维护（SSOT）**，其他文档引用不复制；**领域业务术语在 DOMAIN-MODEL §3 通用语言表维护**（领域特有，引用 GLOSSARY）；③ 别称/易混淆词列出禁止混用；④ **不用 emoji**、无元信息表、无变更记录。

---

## 1. 术语表

> 【指引】按层/域分组列出术语。**通用语言（Ubiquitous Language）**：业务专家与开发用同一套词汇。

### 1.1 产品与功能层

| 术语     | 含义                                              | 别称/易混淆          |
| -------- | ------------------------------------------------- | -------------------- |
| 能力     | 产品能力图节点（产品规格定义，SSOT）              | 功能（易混淆，见下） |
| 功能     | DOMAIN-MODEL 聚合操作(Action)描述的对象           | 能力（易混淆，见上） |
| 能力域   | 能力分组（内容创作/内容加工/账户商业化/作品沉淀） | 模块（禁止混用）     |
| （补充） |                                                   |                      |

### 1.2 架构层

| 术语     | 含义                                                             | 别称/易混淆              |
| -------- | ---------------------------------------------------------------- | ------------------------ |
| 应用     | 可独立部署的单元（前端 / 后端 API）                              | 服务、模块（禁止混用）   |
| 模块     | 应用内部的功能实现单元                                           | 应用（禁止混用）         |
| 领域对象 | 业务建模对象（实体/值对象/聚合根）                               | 数据对象（禁止混用）     |
| 状态机   | 领域对象的完整状态模型（环式网络，DOMAIN-MODEL §3.6 定义，SSOT） | 状态流转（易混淆，见下） |
| 状态流转 | DOMAIN-MODEL §3 Action 流程图画片段                              | 状态机（易混淆，见上）   |
| （补充） |                                                                  |                          |

### 1.3 技术与数据层

| 术语     | 含义                                              | 别称/易混淆      |
| -------- | ------------------------------------------------- | ---------------- |
| 接口     | 对外提供的调用契约（L3 API 定义字段/类型/校验）   | 功能（禁止混用） |
| 部署单元 | 最小可独立构建/发布/回滚的组件（DEPLOYMENT 登记） | 应用（禁止混用） |
| （补充） |                                                   |                  |
```
