# 布局、尺寸与网格（layout & grid）

> 本文件是 c4-container-diagram skill 的「布局/尺寸/网格」详参，由 SKILL.md 的 §6.1/6.2/6.7/6.8/6.9/6.10/6.11/6.13/6.15 拆出。**核心尺寸公式最常用，画图/改图前必读本节 §尺寸计算规则（6.13）**。铁律速查与主流程见 [SKILL.md](../SKILL.md)。

## Contents

- [6.1 外层容器必须 grid-rows:1 grid-columns:1 强制纵向堆叠](#61-外层容器必须-grid-rows1-grid-columns1-强制纵向堆叠)
- [6.2 子模块 width 必须固定（否则右侧大片留白）](#62-子模块-width-必须固定否则右侧大片留白)
- [6.7 容器 width 与 label 长度的关系](#67-容器-width-与-label-长度的关系)
- [6.8 跨平台字体渲染差异](#68-跨平台字体渲染差异)
- [6.9 子容器 width 撑满公式（已并入 6.13）](#69-子容器-width-撑满公式已并入-613)
- [6.10 子容器总数 > grid-columns 会换行（主导方向）](#610-子容器总数--grid-columns-会换行主导方向)
- [6.11 同一列等宽约束](#611-同一列等宽约束)
- [6.13 尺寸计算规则（父容器与子容器，核心约束）](#613-尺寸计算规则父容器与子容器核心约束)
- [6.15 子容器数非 grid-columns 整数倍](#615-子容器数非-grid-columns-整数倍)

---

## 6.1 外层容器必须 `grid-rows:1 grid-columns:1` 强制纵向堆叠

ELK/dagre **不会自动纵向堆叠无连接的独立子容器**——实测会横向并排。

**必须**用外层 wrapper 容器：

```d2
整体架构: {
  grid-rows: 1        # ← 关键
  grid-columns: 1    # ← 关键
  grid-gap: 24

  层1: { ... }
  层2: { ... }
  层3: { ... }
}
```

---

## 6.2 子模块 `width` 必须固定（否则右侧大片留白）

**实测陷阱**（本机 D2 v0.8.1 + ELK 验证）：grid 列宽**默认由该列内容宽度决定，不会自动均分容器宽度**。

- ❌ 不设 width：3 个模块层 = 4 个模块层宽度不同 → 视觉参差；2 个模块层右侧留白 60%
- ✅ 设 width（按 §6.13 公式）：所有模块完全等宽且撑满父容器，等距 gap 均匀分布

```d2
入口层: {
  width: 1000
  grid-columns: 3
  h1: { width: 317; height: 60; class: module }    # ← 固定 width 必须（§6.13: (1000−24−24)/3 = 317）
  h2: { width: 317; height: 60; class: module }
  h3: { width: 317; height: 60; class: module }
}
```

---

## 6.7 容器 `width` 与 `label` 长度的关系

带 label 的容器，`width` 设的是**内容区宽度**，label 会**额外撑大容器实际宽度**。所以如果设 `width: 1000` 但 label 是"② 业务能力层（中台 · 能力复用）"这种长文本，渲染后容器实际宽度可能 1500+。

**规避**：label 保持简短（"② 业务层"），或对宽 label 容器不固定 width。

---

## 6.8 跨平台字体渲染差异

中文字符在 ELK/dagre 下按 2 列宽处理，ASCII 输出时字符间会被插入对齐空格（如"应用"渲染为"应 用"）。**自检注意事项见 troubleshooting.md 验收执行**（用 cat 全文阅读，不要 grep 中文字面）。

---

## 6.9 子容器 `width` 撑满公式（已并入 6.13）

> 公式与速查表已并入 [6.13 尺寸计算规则](#613-尺寸计算规则父容器与子容器核心约束)（确定性公式 + 折叠速查表 + 边界约束）。本条仅保留关键认知。

**关键认知**：撑满与否取决于**每行的列数 N**（即 grid-columns 值），不是子容器总数。子容器总数 > N 时会换行（见 6.10），每行宽度由 N 决定。

---

## 6.10 子容器总数 > `grid-columns` 会换行（主导方向）

`grid-columns: 3` 放 6 个子容器 = 3 列 × 2 行，自动换行。先写 `grid-rows` → 行主导（先填满行）；先写 `grid-columns` → 列主导（先填满列，默认视觉横向）。

**核心认知（影响宽度计算）**：想等宽撑满时，**N 必须取每行列数**（如 6 子想要 2 行 3 列 → `grid-columns: 3`，width 按 §6.13 算 317），不是子容器总数。

---

## 6.11 同一列等宽约束

grid **同一列的单元格等宽、同一行的等高**（取该列/行最大者）。所以同一分区内的子容器 `width` 必须一致，否则窄盒会被宽盒所在的列撑宽，产生留白参差。多个分区并列时，各分区的 `grid-columns` 和子容器 width 应保持一致。

---

## 6.13 尺寸计算规则（父容器与子容器，核心约束）

**⚠️ 超界排查顺序（改图时必读，防止盲目调宽度）**：公式**本身可靠**（实测父 1000：3 列 317、4 列 235 均精确不超界）。渲染后超界时按顺序排查，**不要逐个容器猜宽度**：

1. **第一嫌疑：label 撑宽**（最常见）——子容器/层容器的 width 是**内容宽**，长 label 会把**实际渲染宽**撑超（§6.17）。先给 label `\n` 换行（每行 ≤8 字）或缩 label，再验证
2. **第二嫌疑：列数 N 取错**——N = **每行列数**而非子容器总数（§6.10）；子容器数 ≠ N 时布局残缺（§6.15）
3. **最后才验公式**：`(父宽−24−(N−1)×gap)/N` 或速查表；超界幅度 ≤ 10px 时检查是否长 label 撑宽了几个 px

**两条硬约束**（任何子容器的任何边/角都不能违反）：

1. **水平：子容器总宽 ≤ 父容器内容宽**。子容器宽度必须按公式计算（不能拍脑袋）：

   ```
   子容器 width = (父容器 width − 内边距 − (N − 1) × grid-gap) / N
   ```

   其中 N = 该行子容器个数（= grid-columns 值），内边距 ≈ 24。**速查表见下方**（父 1000 / gap 12 / 内边距 24 的 N=2~6 对应宽度）。

   如果算出的 width 放不下文本（子容器文本溢出），**禁止超宽硬塞**——见 d2-syntax-cheatsheet 的 6.14 trade-off。

2. **垂直：子容器在父容器内均匀分布，不能挤在任意一侧**（ELK 实测结论；垂直居中仅单列容器要求，见 SKILL.md §1.3 铁律 2）：

   **背景**：竖条/标签列等父容器的高度往往由外层 grid 决定（与主体现高），远大于子容器内容自然高。ELK 不会自动把子容器拉伸填满超高容器，需要手算。实测六种写法效果：

   | 写法                                                                            | 效果                                                                                                    |
   | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
   | **外层 grid 内**（竖条与主体并排）：竖条**不设** height + 子容器按公式设 height | ✅ **最佳**：上下留白对称、均匀填满、不超界（竖条 height 会被外层 grid 覆盖，写了也白写）               |
   | **独立竖条**（不在外层 grid 内）：竖条设 `height` + 子容器按公式设 `height`     | ✅ 上下留白对称、均匀填满、不超界                                                                       |
   | 竖条不设 height + 无 `grid-gap`（一维 grid）                                    | ✅ 内容自然高、均匀居中（容器不超高时）                                                                 |
   | 竖条设 `height` + 子容器不设 height                                             | ⚠️ 居中但留白（ELK 扩展有上限）                                                                         |
   | 竖条写 `grid-gap`（任何情况）                                                   | ❌ 子容器紧凑排布**挤在顶部**（用户踩坑点：竖条内写 `grid-gap: 8` → 6 个子容器全挤上方、下方 2/3 空白） |
   | 子容器 height 算大了                                                            | ❌ **超出父容器底部**（违反边界约束）                                                                   |

   **竖条/标签列正确写法（推荐）**：

   ```
   子容器 height = (竖条实际高度 − 上下内边距 − (M − 1) × 间距) / M
   ```

   其中 M = 子容器个数，上下内边距 ≈ 120（含容器标题区），间距 ≈ 40（ELK 默认行间距）。

   ⚠️ **竖条实际高度**：竖条/标签列与主体同在外层 grid 时，**高度由外层 grid 强制与主体现高**（写 `height` 会被覆盖，实测 863 vs 设定的 600）。所以**外层 grid 内的竖条不要设 height**，子容器 height 按**外层 grid 决定的实际高度**算。例：竖条实际高 863、6 个子容器 → (863−120−5×40)/6 ≈ 90（已实测：6×90 高子容器上下各留白 60，完美对称）；实际高 600、4 个子容器 → (600−120−3×40)/4 = 90。
   若不确定实际高度，先用**无 height 版**渲染量一下容器高度，再按公式回填子容器 height；或子容器不设 height 接受"内容自然高"（容器不超高时同样均匀居中）。

   ⚠️ **竖条/标签列：不设 width，让 ELK 自动包裹居中（v0.8.1 实测铁律）**：单列容器（grid-columns:1）内子容器 x 是固定值（约 160，非"父x+60"）。**竖条不要设 width**——ELK 按"子容器宽度 + 两侧等边距"自动包裹竖条，子容器天然居中（实测：左距 60 = 右距 60）：

   ```
   # ✅ 竖条不设 width（推荐，v0.8.1 实测居中）
   共享业务服务层: {
     label: "③ 共享业务服务层"
     grid-columns: 1
     s1: { label: "用户体系"; width: 160; height: 90 }
     s2: { label: "算力体系"; width: 160; height: 90 }
   }

   # ❌ 旧写法：竖条设 width → v0.8.1 子容器 x 固定 160 会贴右（右距≈0）
   共享业务服务层: {
     width: 300
     grid-columns: 1
     s1: { label: "用户体系"; width: 180 }  # 贴右, 左距140 右距0
   }
   ```

   - grid 只定义一维（竖条写 `grid-columns: 1`，**不要写 `grid-rows: 1; grid-columns: 1` 两维**，两维都定义时子容器挤在顶部）。
   - **竖条内不要写 `grid-gap`**（触发"紧凑排布不扩展"）。
   - **竖条内子容器 width 可设可不设**：不设则按内容宽 + 自动居中。
   - **横排场景（普通层容器，铁律 2 同样适用）**：层容器（width:W）内 `grid-columns: N` 铺子容器时，**必须按确定性公式算子容器 width**（实测精确规律，见下）。

   **确定性公式（实测校准）**：

   > ⚠️ **两种内边距口径，用途不同，不要混用**：
   >
   > - **24px**（左 12 + 右 12）= **设计留白**，用于"视觉上撑满且左右留白"的**宽度计算**：`子width = (父宽 − 24 − (N−1)×gap) / N`
   > - **4px** = **ELK 居中安全阈值**，用于**超界判断**（ELK 实测：子总宽 > 父宽−4 时挤出右边界）：`Σ子宽 + (N−1)×gap ≤ 父宽 − 4`
   >   算 width 用 24，判断超界用 4，二者不冲突。速查表（下方折叠表）用的就是 24 口径。

   <details>
   <summary><b>速查表（设计宽度，24 口径；父容器 width:1000，gap:12，内边距≈24）</b></summary>

   | 每行列数 N | 子容器 width | 每行总宽 |
   | ---------- | ------------ | -------- |
   | 2          | 482          | 976      |
   | 3          | 317          | 975      |
   | 4          | 235          | 976      |
   | 5          | 185          | 973      |
   | 6          | 152          | 972      |

   其他父容器宽度：子容器 width 按比例缩放即可（如父 780 → (780−24−24)/4 = 183）。**超界判断**（子总宽是否挤出）用上方"父宽−4 阈值"，本表只算设计宽度。

   </details>

   ```
   A. 多列容器 (grid-columns: N, N ≥ 2):
      ELK 行为: 整组子容器自动居中 (左距 = 右距)
      安全条件: Σ子width + (N−1)×grid-gap ≤ 父宽 − 4
      # 注意: 这是"超界临界值"判断 (ELK 实测阈值), 不是设计宽度!
      # 实际设计宽度用速查表公式: 子width = (父宽 − 24 − (N−1)×gap) / N
      验证: 父340, N=3, gap=4 → 设计宽度 (340−24−8)/3 = 102.7 → 102
            3×102+8=314 ≤ 336 ✓ 不超界; 若按临界值 109 → 3×109+8=335 仍 ≤336
            # 102 (设计) 与 109 (临界) 都在安全区, 但设计宽度用 102 才有左右留白

   B. 单列容器/竖条 (grid-columns: 1) — **实测 v0.8.1 修订版**：
      ELK 行为 (v0.8.1-HEAD): 子容器 x 是固定值 (约全局 160, 非"父x+60"), 独立一行
      ⚠️ **竖条不要设 width** — 让 ELK 自动按"子容器宽度 + 两侧等边距"包裹竖条
         → 子容器天然居中 (实测: 左距 60 = 右距 60 完美对称)
      ❌ 旧写法(旧版 ELK): 竖条设 width + 子容器设 width=竖条宽−120 → v0.8.1 下
         子容器 x 固定 160 会贴右 (右边距≈0, 左边距≈140, 严重不对称)
      例: 竖条 { grid-columns: 1 } (无 width) + 子容器 { width: 160; height: 90 }
         → 竖条按 160+等边距 自动包裹, 子容器居中
      注意: 竖条内子容器 width 可设也可不设; 不设则按内容宽, 自动居中
      注意: 嵌套在多列 grid 内的单列容器同规则 — 不设 width, 让 ELK 包裹

   C. 多列/分区场景：每个子容器都要显式写 width (含嵌套分区), 不能只写 grid-columns
      (例外1: 单列竖条内子容器 width 可设可不设, 见 B)
      (例外2: 嵌套分区容器本身可不设 width——按内容定列宽 §6.2/templates，
       但其内部子容器仍需显式 width)
   ```

   **验证方法**：写代码前先按公式算好（多列居中 / 单列竖条自动包裹），渲染后对照"子容器最右 ≤ 父容器最右"且左右/上下留白大致对称。**如果超界，按本节超界排查序定位根因（第一嫌疑 label 撑宽 → 第二列数 N → 最后才验公式），再修，不要逐个容器猜。**不对称 = grid 维度写错、width/height 算错、或竖条误写 grid-gap。

   ⚠️ **grid 列等高陷阱（垂直溢出）**：外层 `grid-columns: N` 分栏时，ELK 强制**所有列等高**（取最高列）。若某列内部堆叠的子容器总高 > 其他列，该列内容不会自动压缩，可能**底部超出层容器**。实测案例（支付全景图）：核心层 2 列——左侧"支付核心+商户平台"（2 个分区）vs 右侧"支撑栏"（3 个中心竖排），曾出现支撑栏底部溢出。**规避**：分栏时保证各列内容高度大致均衡（子分区数量/内容量相近），或给矮列补充内容；渲染后必须检查**每列底部 ≤ 层容器底部**。

---

## 6.15 子容器数非 grid-columns 整数倍

- **子容器数 > N**：末元素跨行（列主导）或靠左（行主导，§6.10），布局残缺——设计时尽量让子容器数 = N 的倍数
- **子容器数 < N**：ELK 整组居中、剩余列留白（实测 2 子 + 3 列：子1 x=226 子2 x=518 右列空）——正确做法是 `grid-columns` 改为实际子容器数
- **禁止 `grid-column-span`**（渲染蓝色"2"徽章 + 直角矩形，违反 c4-container-spec §4.8 铁律）
